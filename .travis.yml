sudo: false
language: javascript
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
  - npm serve:test  # start a Web server
  - sleep 3 # give Web server some time to bind to sockets, etc
addons:
  firefox: "52.0"
script:
- npm test
